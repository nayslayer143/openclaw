# design-intel-scout-2026-03-23

**Today's finds:** 5 sites — cyberpunk CSS-only glitch, terraforming nav metaphor, Houdini scroll-parallax, clamp() typographic scaling, inverted grayscale hover

**Standout:** Utopia Tokyo — three-layer glitch text system built entirely in CSS (clip-path polygon + RGB drop-shadow + staggered keyframes) with zero canvas or WebGL, fully reduced-motion compliant

**Brief injection ready:** "Build the hero section using clip-path polygons to fracture the frame into angular shards, layer CSS keyframe scanline and noise animations over a dark base, and trigger full-palette theme flips via data-attribute cascades through custom properties on hover."

**Full KB:** 13 entries
