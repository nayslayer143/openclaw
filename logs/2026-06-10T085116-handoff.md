# Handoff — foyer-stage — 2026-06-10

## Goal
Build out the foyer-stage megaplan phase by phase, crossing off checkboxes in `.plans/2026-06-09-megaplan-foyer-stage.md` (the GitHub reference point). P0 (buildable parts) + P1 + P2 are SHIPPED and crossed off. **Next: Phase 3 — Ceremony & craft.**

## Repo
`/Users/nayslayer/code/claw-core/openclaw` · `feat/foyer-stage-portal` @ `76772a0` · dirty (pre-existing submodule drift + untracked `.claude/handoff.md` — leave both) · push: `git pushall`
Sibling: `/Users/nayslayer/code/claw-research/client-mcp` · `master` @ `e178613` · **NO git remotes (local only)**

## Next Action
Open `.plans/2026-06-09-megaplan-foyer-stage.md` §7 Phase 3, start box 1: approval ceremony v2 (receipt moment in rail + soft chime + thread artifact) in `dashboard/clientmcp/portal.html` — `wireReview()` approve handler + `railFor()`. QA via launch.json server `clientmcp-api` (:8771).

## Landmines
- **Live dashboard serves this working tree.** Portal exists only on `feat/foyer-stage-portal`; `main` is 6 weeks stale (`5072d89`) — do NOT switch branches; landing on main = deliberate merge decision for Jordan (megaplan P0 amended box).
- **Dashboard restart ONLY** `launchctl kickstart -k gui/$UID/com.openclaw.dashboard` — never kill the PID (launchd respawns stale code). server.py proxy: `*-stream` paths stream, everything else buffers (90s).
- **Cloudflare buffers `application/x-ndjson`** — stream endpoints MUST use SSE framing (`text/event-stream`, `data: {...}` + blank line). Probe: `curl -sN https://clientmcp.asdfghjk.lol/clientmcp/api/v1/demo/ping-stream` → ticks 700ms apart = healthy.
- **naming-stream rate bucket is shared**: Fastify sees 127.0.0.1 for all public users (no trustProxy). 8/10min cap. Your own tests burn it — wait the window or use ping-stream. Real fix: X-Forwarded-For + trustProxy.
- **Never headless-screenshot the live subdomain.** QA loop = `scripts/portal-qa-proxy.py` (launch.json `clientmcp-api`, :8771) — portal + real API isolated.
- Backend `tsx watch` hot-reloads on save; mandatory `FOYER_USE_OLLAMA=1 FOYER_OLLAMA_MODEL=qwen2.5:7b` (Anthropic key has no credit; qwen3:32b hangs cold).
- `audit_log` is monthly-partitioned (ADR-009) — decision-insert failures at month boundary = missing partition.
- `pushall` silences GitLab failures (`2>/dev/null || true`); GitLab auth dead (glab oauth `invalid_grant` + SSH `Permission denied`) until cred rotation.
- A spawn-chip session for the middleware fix was started by Jordan but the fix already shipped (`aedc495`) — that worktree is redundant; close it.
- Preview browser is origin-sandboxed (can't navigate to live URLs); `location.reload()` inside preview_eval kills the eval target — reload and inspect in separate calls.

## State
- [done] P0: middleware pure-ASGI `aedc495` (all 5 routes 200 post-kickstart); backend deep-read done.
- [done] P1 honest core: `POST /v1/demo/decision` (audit_log + stage message) `93e5a6a`; portal hydrates approvals from feed `decisions[]`, EMPTY_SEED, seed/live chips, relTime skew `dc3e99f`. Cold-load verified (3 decisions hydrate server-side).
- [done] P2 alive: llm.ts `onToken` → `/v1/demo/naming-stream` SSE → portal wire view `df23caf`/`7481027`; edge-buffering found + fixed via SSE `e178613`/`d77c217`; `/v1/demo/warmup` on boot (warm gen ~2–3s); honest presence dot; "since you last looked" greeting; 15s visibility-aware refresh. All verified.
- [done] Earlier same branch: QA fixes `9189439`, Knock-at-the-Threshold easter egg `8676637` (scroll-bounce 7× → Back Room, FIRSTLIGHT pass; side door = triple-click brandmark).
- [blocked] User: GitLab re-auth + ~25-cred rotation (memory `project_openclaw_env_bak_leak`); `main` merge decision; client-mcp remotes.

## Decisions (load-bearing)
- Event log is the product: decisions = append-only `audit_log` events (actor `client_user`, action `update`, target `stage_deliverable`, diff carries payload) + surfaced as `stage`-platform `inbound_messages` — portal is a thin renderer over the feed.
- Show depth, not delay: stream raw tokens (wire view) + parse at end — never per-candidate parsing of partial JSON, never fake slowness.
- Portal stays single-file zero-build until P5 (MCP work is the split seam).
- Delight never gates function (threshold knock lives at scroll edges only).
- "Live conversation via SSE" shipped as visibility-aware 15s polling; true SSE feed is a trivial follow-up (proxy + framing proven).

## Key Paths
- `/Users/nayslayer/code/claw-core/openclaw/dashboard/clientmcp/portal.html` — the Stage (hash routes `#/stage/<id|slug>`, `#/review/<id|slug>/<delivId>`; themes `?v=atrium|nocturne|doorway`)
- `/Users/nayslayer/code/claw-core/openclaw/dashboard/server.py` — `~:105-140` ASGI subdomain router; `~:3054-3110` clientmcp API proxy (stream branch)
- `/Users/nayslayer/code/claw-core/openclaw/.plans/2026-06-09-megaplan-foyer-stage.md` — plan of record (checkbox tracking)
- `/Users/nayslayer/code/claw-research/client-mcp/apps/server/src/routes/demo-agents.ts` — naming-stream/ping-stream/warmup + rate/spend gates
- `/Users/nayslayer/code/claw-research/client-mcp/apps/server/src/routes/demo.ts` — decision endpoint + feed
- `/Users/nayslayer/code/claw-research/client-mcp/packages/agents/src/llm.ts` — `onToken` streaming seam

## References
- Live portal: https://clientmcp.asdfghjk.lol/clientmcp/portal.html?v=nocturne#/stage/foyer-co
- Megaplan on GitHub: https://github.com/nayslayer143/openclaw/blob/feat/foyer-stage-portal/.plans/2026-06-09-megaplan-foyer-stage.md
