# Handoff — foyer-stage-portal — 2026-06-06

## Goal
Stand up Foyer's client portal — "the Stage" (the client-facing projection of agency work) — at clientmcp.asdfghjk.lol and make the entry→review→approve workflow testable. (Session also: health-swept the clientmcp demo links and scrubbed the .env.bak leak from 3 branches — both done.)

## Repo
`/Users/nayslayer/code/claw-core/openclaw` · `feat/ohyeah-subdomain` @ `af62178` · dirty (portal.html UNTRACKED + pre-existing submodule drift) · push: `git pushall`

## Next Action
Decide where to commit `dashboard/clientmcp/portal.html` — it's untracked on the WRONG branch (see landmine 1). Then finish visual QA: open https://clientmcp.asdfghjk.lol/clientmcp/portal.html → click Foyer Co → open "Naming shortlist" → "Generate the shortlist" (live agent) → check Atrium + Doorway themes via ✦ switcher.

## Landmines
- **openclaw branch switched mid-session by a parallel Claude session.** Now `feat/ohyeah-subdomain` @ af62178 (was `feat/controlledchaos-sub-page-2026-05-20` @142318b). `portal.html` is untracked so it survived the checkout — but do NOT commit it onto feat/ohyeah-subdomain (that's someone else's ohyeah prototype). Commit on a dedicated branch, e.g. `git switch -c feat/foyer-stage-portal`.
- **Dashboard `_subdomain_root_router` middleware crashes on client disconnect.** `server.py:115-127` (Starlette BaseHTTPMiddleware) raises `RuntimeError: No response returned.` when a client drops mid-request → transient Cloudflare **502** on clientmcp.asdfghjk.lol / dossier. NOT fixed. Fix = wrap `call_next` in try/except or drop BaseHTTPMiddleware. Crash log: `<root>/logs/dashboard.log`.
- **Never headless-screenshot the LIVE dashboard subdomain** — killing Chrome mid-load triggers the 502 above (that's what caused this session's 502). Screenshot via an isolated `python3 -m http.server <port> --directory <root>/dashboard/clientmcp` (portal renders with seed/fallback data; `/clientmcp/api/*` 404s there but the UI is seed-driven). Only `/tmp/p-entry.png` survived this session (entry/Nocturne — verified good); both render jobs auto-backgrounded then got killed on session resume.
- **Bash tool auto-backgrounds ~30s+ commands and they get KILLED on session resume.** Keep render/screenshot commands short, or isolated so a kill is harmless.
- **Foyer backend (:4000) dies intermittently** → `/clientmcp/api/*` returns 503 `{"error":"foyer-backend-unreachable"}`. Restart: `cd /Users/nayslayer/code/claw-research/client-mcp && docker compose up -d && (lsof -ti :4000|xargs -r kill -9); nohup env FOYER_USE_OLLAMA=1 FOYER_OLLAMA_MODEL=qwen2.5:7b DATABASE_URL=postgres://clientmcp:clientmcp_dev@localhost:15432/clientmcp REDIS_URL=redis://localhost:16379 PORT=4000 HOST=0.0.0.0 pnpm -C /Users/nayslayer/code/claw-research/client-mcp server:dev >/tmp/foyer-server.log 2>&1 & disown`.
- **Restart the dashboard with `launchctl kickstart -k gui/$UID/com.openclaw.dashboard`** — NEVER kill its PID (launchd keepalive respawns the old code).
- **No server.py edit/restart needed to change the portal** — it's served by the existing `app.mount("/clientmcp", StaticFiles(html=True))` at `server.py:3062`. Save `portal.html` → live instantly at `/clientmcp/portal.html`.
- **FOYER_USE_OLLAMA=1 + qwen2.5:7b are mandatory** (Anthropic key has zero credit; qwen3:32b hangs cold). LLM POSTs take 10-60s; dashboard proxy timeout already raised to 90s (`server.py:3041`).

## State
- [done] Portal "the Stage" built + deployed: `<root>/dashboard/clientmcp/portal.html` (45KB). 3 screens (threshold/entry → stage dashboard → deliverable review), 3 themes (Atrium/Nocturne/Doorway, ✦ switcher + `?v=`), wired to live agents. Served **200** on subdomain + path-based; live **naming agent verified** returning real candidates through the portal API path.
- [done] Entry screen visually verified (`/tmp/p-entry.png`) — clean, on-brand, both client cards load.
- [done] Dashboard 502 diagnosed + resolved (kickstart); subdomain back to 200; demo links green.
- [done] `.env.pre-phase2-2026-05-11.bak` leak scrubbed from all 3 branches across local+github+gitlab+gitea (0 dirty refs).
- [wip] Visual QA incomplete — only entry/Nocturne eyeballed; Stage + Review screens and Atrium + Doorway themes not yet screenshot-confirmed (functionally wired).
- [wip] `portal.html` uncommitted + on the wrong branch (landmine 1).
- [blocked] ~25 leaked creds (incl GITHUB_PAT + GITLAB_PAT) STILL NOT ROTATED — scrub != un-leak. See memory `project_openclaw_env_bak_leak`.

## Decisions (load-bearing)
- "Client portal" = Foyer's own **"Stage"** concept (client-facing projection) — per the product's design language in `c.html`.
- Built as a single static file in the dashboard (matches landings a-e), not a separate app — fastest testable deploy, no build step, no restart.
- 3 themes via CSS-variable switcher (`?v=` + localStorage), not 3 files — Aryze/SHIFT pattern.
- Per-client seed data for deliverables/brand/brief (Foyer Co `44444…`, Coven `55555…`); conversation is LIVE from `/demo/feed` filtered by `clientContainerId`.
- justdoit mode: skipped the brainstorming design-approval gate per explicit `/justdoit`; A/B/C delivered as live clickable themes, not mockups.

## Key Paths
- `<root>/dashboard/clientmcp/portal.html` — the portal (NEW, untracked)
- `<root>/dashboard/server.py` — `:115-127` `_subdomain_root_router` (502 culprit); `:3041` proxy 90s; `:3062` static mount; `:3011-3021` a-e routes; `:2998` CLIENTMCP_DIR
- `<root>/dashboard/clientmcp/c.html` — `<script>` has verbatim agent API contracts (naming/qa/brand-ingest POST shapes)
- `/Users/nayslayer/code/claw-research/client-mcp` — Foyer Fastify backend (:4000) + restart playbook
- `<root>/logs/dashboard.log` — dashboard crash log

## References
- Portal: https://clientmcp.asdfghjk.lol/clientmcp/portal.html (`?v=atrium|nocturne|doorway`)
- Landings + chooser: clientmcp.asdfghjk.lol/clientmcp/ and /clientmcp/{a..e}
